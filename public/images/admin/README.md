# Admin media assets

Place sales-demo imagery here. Paths are referenced from `src/config/admin.ts`.

```text
admin/
├── hero/         # Featured hero carousel (high-res)
├── properties/   # Property card thumbnails
└── avatars/      # Agent / contact profile photos
```

Suggested naming: kebab-case Persian transliteration, e.g. `niavaran-penthouse.jpg`.
